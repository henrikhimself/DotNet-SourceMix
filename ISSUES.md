# Issue 1
When the user presses Tab to cycle through all tabs on the Files wizard step, the system stops displaying the application heading. The system must always display the application heading.

# Issue 2
When the user presses Tab to cycle through all tabs on the Files wizard step, the system renders each tab one line lower than the previous one. The system must display the tab line on the same line.

